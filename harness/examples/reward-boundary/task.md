# Verify evidence-bound answers

The repo scores a generated answer against a source sentence.

Accept only output that gives the exact answer and cites explicit evidence copied from the source. Reject outputs that rely on unsupported prose even when the answer text looks right.

Make the verifier pass without changing the verifier.
