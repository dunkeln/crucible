# Case 1B: Raw Codex Math RLVR

## Goal Prompt

```text
/goal Build `examples/b_math_rlvr_raw` as a standalone uv project without using Crucible.

Create a minimal exact-answer math RLVR scaffold directly: dataset, reward function, verifier/check command, and a smoke command. Keep it runnable with uv. Do not use the Crucible harness, plugin skill, recipes, or `.crucible` artifacts.

Report:
- files created
- verifier/check command
- how reward is represented
- what evidence proves it works
- total input/output token usage if available
```

## Expected Proof

Codex owns the scaffold shape directly. The run should show how much structure Codex has to invent without a harness contract.

## Measurement

```text
input_tokens:
output_tokens:
files_created:
check_command:
reward_shape:
evidence:
```
