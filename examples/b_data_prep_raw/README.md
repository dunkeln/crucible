# Case 2B: Raw Codex Data Prep

## Goal Prompt

```text
/goal Build `examples/b_data_prep_raw` as a standalone uv project without using Crucible.

Create a minimal data-prep example directly: messy input data, normalization script, schema/data-quality check, and a smoke command. Keep it runnable with uv. Do not use Crucible, `.crucible`, recipe contracts, or plugin guidance.

Report:
- files created
- source shape
- check command
- how pass/fail is represented
- what evidence proves it works
- total input/output token usage if available
```

## Expected Proof

Codex owns the data-prep shape directly. The run should expose whether it creates a compact check or grows into ad hoc pipeline work.

## Measurement

```text
input_tokens:
output_tokens:
files_created:
source_shape:
check_command:
pass_fail_shape:
evidence:
```
